<section class="all-products">
  <h1 class="">{{ collections['test-collection'].title }}</h1>
  <div class="product-container">
    <div class="product-wrapper">
      {% for product in collections['test-collection'].products %}
        <h3>{{ product.title | link_to: product.url }}</h3>
      {% endfor %}
    </div>
  </div>
</section>

{% style %}
  a {
    text-decoration: none;
    color: ;
  }
{% endstyle %}

{% schema %}
{
  "name": "all-products",
  "settings": [
    {
      "type": "text",
      "id": "header",
      "label": "Header"
    }
  ]
}
{% endschema %}
